bridge.api-key=test-api-key
# Optional since your producer has a defaultValue, but setting it avoids surprises:
bridge.base-url=https://example.invalid

# If your test injects beans that require these too, add them:
AWS_REGION=us-east-1
pagination.encryption-key=test-encryption-key
dynamodb.table.customers=customers-table
dynamodb.table.customer-identities=customer-identities-table